<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/normal_preview"
        android:layout_width="@dimen/image_item_width"
        android:layout_height="@dimen/image_item_height"
        android:contentDescription="@string/normal_preview_description"
        android:layout_gravity="center"
        android:scaleType="centerCrop"
        android:padding="@dimen/img_preview_border"
        android:cropToPadding="true" />
    <!-- android:background="@drawable/img_bg_selector" -->

</FrameLayout>
